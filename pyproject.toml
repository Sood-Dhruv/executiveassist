[project]
name = "executiveassist"
version = "0.1.0"
description = "Executive Assistant AI Agent"
authors = [{name = "Dhruv"}]
dependencies = [
    "openai",
    "fastapi",
    "uvicorn",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"